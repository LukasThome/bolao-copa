# Supabase PostgreSQL
# Encontre em: Settings → Database → Connection string → URI
DATABASE_URL="postgresql://postgres:[SUA_SENHA]@db.[REF].supabase.co:5432/postgres"

# NextAuth — gere com: openssl rand -base64 32
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"

# Google OAuth — console.cloud.google.com → Credenciais → Criar ID do cliente OAuth
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Opcional — importação de jogos via football-data.org
FOOTBALL_DATA_API_KEY=""
